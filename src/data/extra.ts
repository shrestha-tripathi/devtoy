import type { ToolPage } from "./tools";

/** Intent-specific landing pages that reuse an existing tool module. */
export const extraPages: ToolPage[] = [
  {
    slug: "json-validator",
    tool: "json",
    navLabel: "JSON Validator",
    title: "JSON Validator – Check JSON Syntax Online (Offline)",
    h1: "JSON Validator",
    metaDescription: "Check whether JSON is valid and see the exact parser error. Strict RFC 8259 validation in your browser; nothing is uploaded.",
    intro: "Paste a config file, API response or payload and find out immediately whether it is valid JSON. The validator uses your browser's native JSON.parse, which follows the strict JSON grammar, so anything it accepts will also be accepted by standard JSON parsers in other languages.",
    steps: [
      "Paste the JSON you want to check into the input box.",
      "If it parses, the output shows the highlighted document and tree view — that means it is valid.",
      "If it fails, read the error message; it names the unexpected token and usually its position.",
      "Fix the reported problem (often a trailing comma or a single quote) and the result updates as you type.",
    ],
    tips: [
      "A trailing comma after the last item of an object or array is the most common reason JSON is rejected.",
      "Valid JavaScript object literals are not always valid JSON: keys and strings must use double quotes.",
    ],
    faqs: [
      { q: "What standard does it validate against?", a: "Strict JSON as defined by RFC 8259 / ECMA-404, via the browser's built-in parser. Comments, trailing commas, NaN and unquoted keys are errors." },
      { q: "Does it validate against a JSON Schema?", a: "No. It checks syntax only. It does not check your data against a JSON Schema document." },
      { q: "Why does the error position look slightly off?", a: "Error wording and positions come from your browser's JSON engine, so they differ slightly between Chrome, Firefox and Safari. The problem is usually at or just before the reported spot." },
    ],
    related: ["json-formatter", "json-minifier"],
    preset: '{"name": "devtoy", "valid": true, "tags": ["json", "validator"],}',
  },
  {
    slug: "json-minifier",
    tool: "json",
    navLabel: "JSON Minifier",
    title: "JSON Minifier – Compress JSON to One Line (Offline)",
    h1: "JSON Minifier",
    metaDescription: "Minify JSON by removing all whitespace and line breaks. Shrinks payloads for APIs, env vars and fixtures. Runs locally in your browser.",
    intro: "Minifying JSON strips indentation, spaces and newlines that parsers ignore, producing a single compact line. That is handy for embedding JSON in an environment variable, a query string, a test fixture or a curl command, and it trims payload size before sending data over the wire.",
    steps: [
      "Paste formatted JSON into the input box.",
      "Check the output confirms the JSON is valid — invalid JSON cannot be minified safely.",
      "Click Minify to collapse the document onto a single line.",
      "Click Copy to copy the compact result.",
    ],
    tips: [
      "Minifying never changes values: whitespace inside string values is preserved.",
      "If the payload goes over HTTP with gzip or brotli, the saving from minifying is smaller than the raw character difference suggests.",
    ],
    faqs: [
      { q: "Does minifying change my data?", a: "No. Only insignificant whitespace between tokens is removed. Keys, values and order stay the same." },
      { q: "Can I turn minified JSON back into readable JSON?", a: "Yes — click Beautify to re-indent it, or use the JSON Formatter page." },
      { q: "Is the minified JSON sent to a server?", a: "No. Minification uses JSON.parse and JSON.stringify in your browser tab." },
    ],
    related: ["json-formatter", "json-validator"],
    preset: '{\n  "user": {\n    "id": 42,\n    "roles": ["admin", "editor"]\n  },\n  "active": true\n}',
  },
  {
    slug: "jwt-expiry-checker",
    tool: "jwt",
    navLabel: "JWT Expiry Checker",
    title: "JWT Expiry Checker – When Does My Token Expire?",
    h1: "JWT Expiry Checker",
    metaDescription: "Paste a JWT to see its exp, iat and nbf claims as readable dates and check whether it has expired. Decoded locally; tokens stay in your browser.",
    intro: "When an API suddenly returns 401, the first question is whether the access token has expired. Paste the token here to decode its payload and read the exp (expires at), iat (issued at) and nbf (not before) claims. These are Unix timestamps in seconds, shown as human-readable dates you can compare against the current time.",
    steps: [
      "Copy the token from your Authorization header (the part after 'Bearer '), a cookie or local storage.",
      "Paste it into the input box.",
      "Look at the payload for the exp claim and its readable date.",
      "Compare exp with the current time; if it is in the past, the token has expired and needs refreshing.",
    ],
    tips: [
      "exp is in seconds, not milliseconds. A value around 1.7 billion is a date in 2023–2024.",
      "Servers often allow a small clock skew (commonly a minute or so), so a token may still be accepted just after exp.",
    ],
    faqs: [
      { q: "Does this verify the token's signature?", a: "No. It only decodes the header and payload. An expiry date shown here does not prove the token is genuine." },
      { q: "What if there is no exp claim?", a: "exp is optional in the JWT spec (RFC 7519). Without it the token has no built-in expiry, and validity depends on the server." },
      { q: "Is it safe to paste a production token?", a: "Decoding happens in your browser and the token is not sent anywhere. Still, treat live tokens as secrets and prefer expired or test tokens when you can." },
    ],
    related: ["jwt-decoder", "epoch-to-ist"],
  },
  {
    slug: "base64-to-text",
    tool: "base64",
    navLabel: "Base64 to Text",
    title: "Base64 to Text Decoder (UTF-8, Offline)",
    h1: "Base64 to Text Converter",
    metaDescription: "Decode a Base64 string back to readable UTF-8 text. Handles emails, API fields, basic auth headers and data URIs. Runs in your browser.",
    intro: "Base64 shows up in email bodies, HTTP Basic auth headers, Kubernetes secrets, JWT segments and API fields that carry binary-safe text. Paste the encoded string to turn it back into readable text. Output is interpreted as UTF-8, so accented characters and emoji decode correctly.",
    steps: [
      "Paste the Base64 string into the input box.",
      "Make sure decode mode is selected.",
      "Read the decoded text in the output.",
      "Click Copy to copy the plain text.",
    ],
    tips: [
      "Kubernetes secret values from 'kubectl get secret -o yaml' are Base64 — decode them to read the value.",
      "A Basic auth header decodes to 'username:password', so treat the output as a credential.",
    ],
    faqs: [
      { q: "Why is the output garbled?", a: "The data is probably binary (an image, zip or encrypted blob), not text. This page decodes to UTF-8 text only." },
      { q: "Does it support URL-safe Base64?", a: "URL-safe Base64 replaces + and / with - and _. If decoding fails, swap those characters back and add = padding to a multiple of 4." },
      { q: "Is Base64 encryption?", a: "No. It is a reversible encoding that anyone can decode. It gives no confidentiality." },
    ],
    related: ["base64-encode-decode", "jwt-decoder"],
    preset: "SGVsbG8sIGRldnRveSEgVGhpcyB0ZXh0IHdhcyBCYXNlNjQgZW5jb2RlZC4=",
  },
  {
    slug: "epoch-to-ist",
    tool: "timestamp",
    navLabel: "Epoch to IST",
    title: "Epoch to IST Converter – Unix Time to India Time",
    h1: "Epoch to IST Converter",
    metaDescription: "Convert a Unix epoch timestamp to Indian Standard Time (IST, UTC+05:30) and back. Seconds or milliseconds, calculated locally in your browser.",
    intro: "Server logs, databases and APIs store time as a Unix epoch in UTC, but if you work in India you want to read it as IST. Indian Standard Time is a fixed UTC+05:30 with no daylight saving, so the conversion is always a 5 hours 30 minutes offset from UTC. Paste an epoch value to see it as a date and time.",
    steps: [
      "Paste the epoch timestamp (10 digits for seconds, 13 for milliseconds).",
      "Read the UTC date in the output.",
      "Read the local time, which is IST if your device is set to India time; otherwise add 5:30 to UTC.",
      "To go the other way, enter a date and copy the resulting epoch value.",
    ],
    tips: [
      "IST never changes for daylight saving, so the +05:30 offset is the same all year.",
      "Midnight IST is 18:30 UTC on the previous day — a frequent source of off-by-one-day bugs in reports.",
    ],
    faqs: [
      { q: "What is the IST offset from UTC?", a: "IST is UTC+05:30 all year. The IANA time zone name is Asia/Kolkata." },
      { q: "Why does my timestamp show a date in 1970?", a: "You probably entered milliseconds where seconds were expected, or the other way round. Check the number of digits." },
      { q: "Does this send my timestamp anywhere?", a: "No. Conversion uses the JavaScript Date object in your browser." },
    ],
    related: ["unix-timestamp-converter", "jwt-expiry-checker"],
    preset: "1735669800",
  },
  {
    slug: "regex-cheatsheet-tester",
    tool: "regex",
    navLabel: "Regex Cheatsheet",
    title: "Regex Cheatsheet & Tester – JavaScript Regular Expressions",
    h1: "Regex Cheatsheet and Live Tester",
    metaDescription: "A quick JavaScript regex cheatsheet with a live tester: character classes, quantifiers, anchors, groups and flags, with matches highlighted as you type.",
    intro: "Keep the syntax you always forget next to a live tester. Try each pattern against your own text and see matches highlighted instantly. Everything uses the browser's ECMAScript RegExp engine, so behaviour matches JavaScript and TypeScript code (and differs in places from PCRE, Python or Go).",
    steps: [
      "Pick a pattern from the cheatsheet below, e.g. \\d+ for digits or ^\\w+@\\w+\\.\\w+$ for a simple email shape.",
      "Type it into the pattern field and set flags: g (all matches), i (ignore case), m (multiline), s (dot matches newline), u (Unicode).",
      "Paste your test text and watch matches and capture groups highlight.",
      "Adjust the pattern until it matches exactly what you want, then copy it into your code.",
    ],
    tips: [
      "Classes: \\d digit, \\w word character, \\s whitespace, . any character except newline; capitals (\\D, \\W, \\S) negate.",
      "Quantifiers: * 0+, + 1+, ? 0 or 1, {2,5} range; add ? (e.g. +?) to make them lazy.",
      "Anchors and groups: ^ start, $ end, \\b word boundary, (...) capture, (?:...) non-capturing, (?<name>...) named group.",
      "Lookarounds: (?=...) lookahead, (?!...) negative lookahead, (?<=...) lookbehind, (?<!...) negative lookbehind.",
    ],
    faqs: [
      { q: "Which regex flavour is this?", a: "JavaScript (ECMAScript) RegExp as implemented by your browser. Possessive quantifiers and atomic groups from PCRE are not supported." },
      { q: "Why does my pattern only find one match?", a: "Without the g flag, JavaScript stops after the first match. Enable g to find them all." },
      { q: "Are lookbehinds supported?", a: "Yes, in all current major browsers, including Safari since version 16.4." },
    ],
    related: ["regex-tester", "json-validator"],
  },
];
