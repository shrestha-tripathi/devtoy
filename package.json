{
  "name": "devtoy-native",
  "private": true,
  "version": "1.0.0",
  "description": "Offline Developer Utilities - Your data never leaves your device",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-wasm": "^3.3.0",
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "idb-keyval": "^6.2.1"
  }
}
